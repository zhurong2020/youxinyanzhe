# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.python-version
*.pyc
*.pyo
*.pyd
.Python

# 虚拟环境
env/
venv/
ENV/
.venv/

# 环境变量
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 注意：.env文件包含敏感信息，已排除在版本控制之外

# 日志文件（构建目录.build/logs/已在下方.build/中排除）
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 测试（htmlcov/现在在.build/目录下，已在下方.build/中排除）
tests/test_data/
.pytest_cache/
.coverage
.tox/
.nox/
.coverage.*
nosetests.xml
coverage.xml
*.cover
.mypy_cache/

# 构建和运行时目录
.build/
.tmp/

# 会员系统数据（包含敏感用户信息）
.tmp/member_data/

# YouTube OAuth凭据（敏感信息）
config/youtube_oauth_credentials.json
config/youtube_oauth_token.json

# 视频文件（临时下载，不应进入版本控制）
youtube_videos/
*.mp4
*.mov
*.avi
*.mkv
*.webm

# 数据文件（包含敏感信息和缓存）
_data/image_mappings.yml
_data/github_releases.json
_data/github_token_status.json
_data/email_records.json
_data/pending_reward_requests.json
_data/processed_reward_requests.json

# 归档目录
_drafts/archived/

# VIP素材准备目录（包含敏感商业资料，不应进入版本控制）
_drafts/archived/vip-preparation/

# 测试生成的文档
_drafts/test-article-*.md
_drafts/*auto-test*.md

# 系统文件
.DS_Store
Thumbs.db
*.swp
.*.swp
*~

# 图片处理临时目录 (包含用户数据，不应提交)
assets/images/processing/
assets/images/temp/

# OneDrive图床相关临时目录
temp/image_processing/
temp/recovered_images/
temp/onedrive_snapshots/

# 但保留目录结构说明文件
!assets/images/processing/.gitkeep
!assets/images/processing/README.md
!assets/images/temp/.gitkeep
!temp/image_processing/.gitkeep
!temp/recovered_images/.gitkeep
!temp/onedrive_snapshots/.gitkeep

# 传统posts目录也排除 (已迁移到云端)
assets/images/posts/
!assets/images/posts/.gitkeep

# 音频文件目录（文件较大，不应进入版本控制）
assets/audio/
!assets/audio/.gitkeep

# 其他临时和优化文件
assets/images/*/optimized/
assets/images/*/wechat_optimized/
*-temp.*
*-test.*
*-backup.*

# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
.jekyll-metadata
vendor/
.bundle/

# IDE
.idea/
.vscode/
# Note: If you need VS Code settings for Python path resolution, create a project-specific settings file
*.sublime-project
*.sublime-workspace
.project
.classpath
.settings/
*.iml
*.Zone.Identifier

# Custom configuration files
config/elevenlabs_voices.yml
config/gemini_config.yml
.claude/
# 会员系统安全文件
.tmp/member_data/access_codes_whitelist.json

# OneDrive OAuth凭据（敏感信息）
config/onedrive_tokens.json
config/azure_app_info.md

# 会员系统安全文件
.tmp/member_data/.security_key

# PDF文档（版权保护，避免法律风险）
*.pdf
!docs/*.pdf

# 创作临时目录（包含临时图片和截图，不应提交）
temp/drafting/
scripts/.tmp.backup*/
