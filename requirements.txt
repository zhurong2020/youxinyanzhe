# 基础依赖
python-dotenv>=1.0.0
pyyaml>=6.0.1
requests>=2.31.0  # 用于HTTP请求
pillow>=10.0.0
markdown>=3.5.2
markdown2>=2.4.1 # 用于公众号内容转换
olefile>=0.47
rich>=13.7.0
filetype>=1.2.0
# 注意: uuid, tempfile, shutil等是Python标准库模块，不需要在requirements.txt中列出

# 内容处理
python-frontmatter>=1.0.0
google-generativeai>=0.3.0

# 微信内容变现系统新增依赖
weasyprint>=60.0  # PDF生成
fonttools>=4.42.0  # 字体支持
cffi>=1.15.0  # weasyprint依赖

# 测试
pytest>=7.4.0
pytest-cov>=4.1.0